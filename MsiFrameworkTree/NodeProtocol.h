#pragma once

#include "stdafx.h"
#include "DatabaseInfo.h"

/*
class NodeProtocol {
public:

  // navigation in database
  virtual unique_ptr<NodeProtocol> children(wstring condition) = 0;

  // select operation
  virtual unique_ptr<NodeProtocol> select() = 0;

  // update operation
  virtual bool update(wstring newValue) = 0;

  // delete operation
  virtual bool deleteThis() = 0;

  // getters
  virtual int getInt();
  virtual wstring getWstring();

  // unicorns
  virtual void joinStuff(unique_ptr<NodeProtocol> aNodeProtocol) = 0;

private:

};
*/
